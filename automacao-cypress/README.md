Como instalar o Cyperss ? 

É importante que se tenha instalado o Node e o pacote npm, como eu já tinha instalado, só foi executado o comando abaixo para instalar o Cypress: 

- npm install cypress --save-dev 

Para executar o cypress:

- npx cypress open


