Como instalar o Cyperss ? 

É importante que se tenha instalado antes de se instalar o Cypress o pacote npm, como eu já tinha instalado, só foi executado o comando: 

- npm install cypress --save-dev 

Para executar o cypress:

- npx cypress open


